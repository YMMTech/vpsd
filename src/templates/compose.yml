services:
  app:
    image: "${APP_IMAGE:?APP_IMAGE must be an immutable image reference}"
    env_file:
      - deploy.env
    expose:
      - "${APP_PORT}"
    networks:
      - simploy-ingress

networks:
  simploy-ingress:
    external: true
    name: ${SIMPLOY_INGRESS_NETWORK:-simploy-ingress}
